# TODO: Add Diwali Song to Happy Diwali Page

- [x] Modify index.html to add an `<audio>` element that autoplays and loops the song in the background, placed after the canvas, with `<source src="./diwali.mp3" type="audio/mpeg">` and a fallback message.
